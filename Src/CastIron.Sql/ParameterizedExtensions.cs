using System.Data;

namespace CastIron.Sql
{
    public static class ParameterizedExtensions
    {
        //public static T AddInParameter<T>(this T query, string name, object value, int size)
        //    where T : Parameterized
        //{
        //    query.AddParameter(new Parameter(name, value, ));
        //    return query;
        //}

        //public static T AddOutParameter<T>(this T query, string name, object value)
        //    where T : Parameterized
        //{
        //    query.AddParameter(new Parameter(name, value, ParameterDirection.Output));
        //    return query;
        //}

        //public static T AddInOutParameter<T>(this T query, string name, object value)
        //    where T : Parameterized
        //{
        //    query.AddParameter(new Parameter(name, value, ParameterDirection.InputOutput));
        //    return query;
        //}
    }
}